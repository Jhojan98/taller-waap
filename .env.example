# Copiar a .env (no versionado): cp .env.example .env
JUICE_PORT=3000
WAAP_PORT=8080
PARANOIA=1
# En Mac hacia VM: WAAP_HOST=<IP-VM> (solo informativo, compose usa localhost por defecto)
WAAP_HOST=localhost
