# taller-waap — Fase 0-1

Prototipo WAAP (Juice Shop + ModSecurity CRS). Ver `../PLAN_FASE1.md`.

```bash
cp .env.example .env
docker compose up -d && docker compose ps
curl -i http://localhost:8080
```
